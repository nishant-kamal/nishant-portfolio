// postcss.config.js — no changes needed, this is correct as-is.
// tailwindcss + autoprefixer is the standard Next.js PostCSS setup.

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};












































